# == MULTOVL Dockerfile ==

# The base image
# Always use a concrete tag, not "latest"!
FROM ubuntu:focal-20210416

# Install MULTOVL
# because it is statically built, a simple COPY should be sufficient
# TODO

